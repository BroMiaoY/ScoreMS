#?????
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scorems?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: 464822
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations:
    - classpath*:/mapper/*.xml
  type-aliases-package: artwork.entity.User.User

token:
  secretKey: lab^@#@78*&^os

server:
  port: 8080